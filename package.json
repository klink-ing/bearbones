{
  "name": "klinkcss",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run -r test && vp run -r build",
    "check": "vp run @klinking/panda-markers#build && vp run @klinking/panda-shorthand#build && vp run website#codegen && vp check && vp run -r test",
    "fix": "vp fmt",
    "prepare": "vp config"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@10.33.2"
}
