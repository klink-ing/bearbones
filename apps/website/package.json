{
  "name": "website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "panda codegen && panda --watch & vp dev",
    "build": "panda codegen && panda && tsc && vp build",
    "preview": "vp preview",
    "panda": "panda codegen && panda",
    "codegen": "panda codegen"
  },
  "dependencies": {
    "@klinking/panda-markers": "workspace:*",
    "@klinking/panda-shorthand": "workspace:*",
    "@pandacss/dev": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  }
}
